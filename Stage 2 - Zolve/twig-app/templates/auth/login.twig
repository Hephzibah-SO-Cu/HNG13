{% extends 'base.twig' %}

{% block content %}
  <div class="container" style="max-width: 400px; margin: 4rem auto;">
    <div class="card">
      <h2>Login to Zolve</h2>
      <form action="/auth/process_login.php" method="POST"> {# Absolute path #}
        <input type="email" name="email" placeholder="Email" required>
        <input type="password" name="password" placeholder="Password" required>
        {% if error %}
          <p style="color: red;">{{ error }}</p>
        {% endif %}
        <button type="submit" style="width: 100%; padding: 12px; background: var(--primary); color: white; border: none; border-radius: 8px;">
          Login
        </button>
      </form>
      <p style="text-align: center; margin-top: 1rem;">
        <a href="/auth/signup.php">Create an account</a>
      </p>
    </div>
  </div>
{% endblock %}